@using HuckHack.Domain.Entities
@using HuckHack.Domain.Enums

<partial name="ModalsPartial" />

<div class="container-fluid">
    @if (User.IsInRole(UserRole.Admin.ToString()))
    {
        <div class="card card-cascade cascading-admin-card user-card mt-4">
            <button type="button" id="createEventButton" data-toggle="modal" data-target="#createEventModal"
                    class="btn btn-primary btn-sm col-2 m-4">
                Создать событие
            </button>

        </div>
    }
</div>

<script type="text/javascript">
</script>